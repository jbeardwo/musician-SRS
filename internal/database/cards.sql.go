// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: cards.sql

package database

import (
	"context"
	"database/sql"
	"time"

	"github.com/google/uuid"
)

const createCard = `-- name: CreateCard :one
INSERT INTO cards(
  id,
  front_content,
  back_content,
  interval,
  target,
  ease_factor,
  repetitions_count,
  last_reviewed_at,
  last_reviewed_num,
  created_at,
  deck_id,
  tempo,
  perfect_streak,
  bad_streak
) VALUES (
  gen_random_uuid(),
  $1,
  $2,
  1,
  $3,
  2.5,
  0,
  NULL,
  0,
  NOW(),
  $4,
  $5,
  0,
  0
) RETURNING 
  id,
  front_content,
  back_content,
  interval,
  target,
  ease_factor,
  repetitions_count,
  last_reviewed_at,
  created_at,
  deck_id,
  tempo,
  perfect_streak,
  bad_streak
`

type CreateCardParams struct {
	FrontContent string
	BackContent  string
	Target       int32
	DeckID       uuid.UUID
	Tempo        int32
}

type CreateCardRow struct {
	ID               uuid.UUID
	FrontContent     string
	BackContent      string
	Interval         int32
	Target           int32
	EaseFactor       float64
	RepetitionsCount int32
	LastReviewedAt   sql.NullTime
	CreatedAt        time.Time
	DeckID           uuid.UUID
	Tempo            int32
	PerfectStreak    int32
	BadStreak        int32
}

func (q *Queries) CreateCard(ctx context.Context, arg CreateCardParams) (CreateCardRow, error) {
	row := q.db.QueryRowContext(ctx, createCard,
		arg.FrontContent,
		arg.BackContent,
		arg.Target,
		arg.DeckID,
		arg.Tempo,
	)
	var i CreateCardRow
	err := row.Scan(
		&i.ID,
		&i.FrontContent,
		&i.BackContent,
		&i.Interval,
		&i.Target,
		&i.EaseFactor,
		&i.RepetitionsCount,
		&i.LastReviewedAt,
		&i.CreatedAt,
		&i.DeckID,
		&i.Tempo,
		&i.PerfectStreak,
		&i.BadStreak,
	)
	return i, err
}

const deleteCards = `-- name: DeleteCards :exec
  DELETE FROM cards
`

func (q *Queries) DeleteCards(ctx context.Context) error {
	_, err := q.db.ExecContext(ctx, deleteCards)
	return err
}
